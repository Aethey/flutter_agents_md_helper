# Flutter AGENTS.md Helper

A Flutter Web app that turns project requirements into architecture decisions and exports `FLUTTER_ARCHITECTURE.md` for coding agents.

```bash
flutter run -d chrome
flutter build web --release --base-href "/flutter_agents_md_helper/"
```
