targets:
  $default:
    builders:
      json_serializable:
        options:
          explicit_to_json: true
          checked: true
      freezed:
        options:
          make_collections_unmodifiable: false
